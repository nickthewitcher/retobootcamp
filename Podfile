# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'bootcampreto' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for bootcampreto

  target 'bootcampretoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'bootcampretoUITests' do
    # Pods for testing
  end
  pod 'Alamofire', '~> 5.2'

end
